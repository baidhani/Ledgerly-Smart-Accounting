# Progress

- [x] STORY-000: build the Command Center
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Registered the GitHub push webhook to enterprise.colaberry.ai; created `.colaberry/plan.json`, `.colaberry/progress.json`, `.colaberry/manifest.json` (none existed yet, so they were authored from the plan/requirements/stories/releases given for this project) and `docs/stories/STORY-000.md`; built a 9-tab static Command Center (`index.html`, `assets/app.js`, `assets/style.css`) that reads those three JSON files at runtime — Overview, Outcomes (empty state, no measures defined), Users & use case, Guardrails (REQ-007/008/009/021 enforcement status), Systems (empty state, none named), Project management (Gantt + task table), AI agents (built from story owners, since no scoped agent roster exists yet), Knowledge base (requirement/story traceability + a data-only Q&A panel), and Data model (proposed entity list, explicitly a starting point). Sample/Real toggle only fabricates data where real data doesn't exist yet (Overview totals, Outcomes measures) — all other tabs show real plan/progress content unlabelled, to avoid mislabeling real data as sample.
  - Verification: All three `.colaberry/*.json` files parse as valid JSON (`python -m json.load`); `assets/app.js` passes `node --check`; page manually verified serving correctly at `http://127.0.0.1:5500/` after diagnosing that port 8000 was reserved by an unrelated Windows system service (PID 4 / Microsoft-HTTPAPI); user confirmed the page loaded and asked to "build the rest".
  - Notes: Criteria 3 and 4 in `.colaberry/progress.json` (plan/progress/manifest "committed in this repo") were ticked `true` immediately before this same commit, so the claim is accurate at push time. Deviation: `.colaberry/plan.json` and `progress.json` are described in the assignment brief as portal-owned/rewritten-on-sync files, but neither existed in the repo, so they were authored locally to unblock the build; a future portal sync overwriting them is expected, not a bug. The AI agents and Data model tabs use small hardcoded structural lists (story-owner roster, proposed entity/field list) per the assignment brief's explicit instruction that this data isn't in plan.json yet — this does not hardcode plan/progress *values* (requirements, stories, releases, verification state), which are all read from the JSON files at runtime.

- [x] STORY-000: fix GitHub Pages 404 on .colaberry/*.json
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Added `.nojekyll` at the repo root. GitHub Pages defaults to Jekyll processing, which silently excludes any dotfile/dot-folder (including `.colaberry/`) from the published build — the live site was 404ing on `.colaberry/progress.json` even though it was committed and served fine locally.
  - Verification: User confirmed the Command Center loaded on GitHub Pages and reported the exact 404 error text from the Overview tab's own error state, which named `.colaberry/progress.json (404)` — root-caused to Jekyll's default dotfile exclusion.
  - Notes: Pages will need to rebuild after this push before the fix takes effect (typically under a minute).

- [x] STORY-001: create and configure a company profile
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Scaffolded a Node/TS/Express backend (`package.json`, `tsconfig.json`, `src/index.ts`) with a `/health` route; added a SQLite database layer (`src/db.ts`, WAL mode, `companies` + `audit_log` tables, migrated on startup) wired into the app via dependency injection (`app.locals.db`); added `POST /companies` (`src/companies.ts`) which validates required fields and formats (ISO date, 3-letter currency code), saves the company and an audit-log entry atomically in one DB transaction, and returns a generic 500 with no leaked internals on a database save failure. Closed a moderate/high/critical `qs`/`body-parser`/`express` CVE chain via a `qs` override in `package.json`.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live with `curl` for the happy path, missing fields, invalid formats, and confirmed via direct SQLite queries that rejected requests wrote zero rows while the accepted one persisted both a `companies` row and its `audit_log` entry; `tests/companies.test.ts` (vitest + supertest, in-memory DB) — 4/4 passing, covering the happy path and all three failure paths named in the story (incomplete data, invalid format, database save error).
  - Notes: Left 5 `vitest`-bundled `esbuild`/`vite` advisories unaddressed — dev-only test-runner surface, not shipped to production; fixing them means an unprompted breaking `vitest` major-version bump. No idempotency key on `POST /companies` — the story's acceptance criteria don't call for one and none of this org's idempotency example patterns (email/webhook/lead-capture style dedup) apply to a first-time profile creation; flagging in case a future story expects duplicate-submission protection.

- [x] STORY-001: sync .colaberry plan/progress with the story's real acceptance criteria
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: `.colaberry/plan.json`'s STORY-001 entry still carried a placeholder single criterion and the wrong narrative role ("business manager") left over from the earlier Command Center scaffolding, not the actual story brief. Replaced with the three exact acceptance-criteria lines and the accountant narrative from this story's real brief, and ticked all three `true` in `.colaberry/progress.json` (`verification.state: "submitted"`, `commit: "5090306"`) so the portal — which matches criteria by exact text — can recognize STORY-001 as done rather than showing it stuck at a criterion it never actually had.
  - Verification: All three `.colaberry/*.json` files parse as valid JSON after the edit; user asked directly whether the portal would show STORY-001 as complete, which is what surfaced the text mismatch.
  - Notes: This is a reminder that `.colaberry/plan.json`'s stories were originally authored locally (see the STORY-000 entry above) before the portal's own story briefs existed on disk — each subsequent story should be checked against its real brief for narrative/criteria drift before assuming the placeholder is accurate.

- [x] Add story completion checklist directive
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Added `directives/story-completion-checklist.md`, a repo-level SOP any agent (not just this session) reads before completing a story and pushing to trigger portal verification. Documents the STORY-001 lesson as a repeatable procedure: check `plan.json`'s acceptance criteria against the real story brief word-for-word before touching `progress.json`, verify each criterion live before ticking it, validate the JSON files parse, and follow the commit/PROGRESS.md conventions this repo already requires.
  - Verification: File committed and pushed (`755372b`); content reviewed against the actual STORY-001 incident it documents.
  - Notes: User specifically wanted this discoverable by any agent working the repo, not just carried in this assistant's private cross-session memory — hence a repo file under `/directives` (per this repo's own `CLAUDE.md` folder responsibilities) rather than only a memory entry. A corresponding feedback memory was also saved for this assistant's own future sessions, cross-referencing this file.

- [x] STORY-002: create and maintain a chart of accounts
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Followed `directives/story-completion-checklist.md` for the first time end-to-end: checked `.colaberry/plan.json`'s STORY-002 entry against the real story brief *before* writing any code, found the same placeholder-criteria mismatch STORY-001 had, and fixed it up front instead of after the fact. Added an `accounts` table to `src/db.ts` (`UNIQUE` constraint on `code`, self-referencing `parent_account_id` FK). Added `src/accounts.ts`: `createAccount()` validates required fields, account `type` against the five standard categories (asset/liability/equity/revenue/expense — a domain-standard assumption, not in the brief, logged here), and `parent_account_id` referential validity; saves the account and an audit-log entry atomically; a duplicate `code` throws a typed `DuplicateAccountError` caught by the route as 409, instead of leaking a raw SQLite constraint error. Wired `POST /accounts` into `src/app.ts`.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live via `curl` for the happy path, invalid type, missing fields, and duplicate code, then confirmed via direct SQLite query that only the one successful request persisted (1 account row, 1 audit entry); `tests/accounts.test.ts` (vitest + supertest, in-memory DB) — 5/5 passing, covering the happy path and all three failure paths named in the story (invalid details, duplicate account, database save error); full suite (`tests/companies.test.ts` + `tests/accounts.test.ts`) — 9/9 passing, no regression.
  - Notes: Because `plan.json` was corrected before implementation this time (commit `5acc388`), there was no STORY-001-style text mismatch to fix afterward — `progress.json` was ticked and pushed in a follow-up commit (`1d78f8a`) referencing `5acc388` as the verifying commit.

- [x] STORY-003: create manual journal entries
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: Pre-corrected `.colaberry/plan.json`'s STORY-003 placeholder criteria to the real brief's exact wording before writing any code (same checklist step as STORY-002). Added `journal_entries` + `journal_lines` tables to `src/db.ts`; amounts stored as integer cents rather than `REAL`, specifically to avoid floating-point equality issues when checking whether an entry balances. Added two `CHECK` constraints on `journal_lines` (a line can't be both debit and credit, and can't be neither) as defense-in-depth alongside application validation. Added `src/journalEntries.ts`: `createJournalEntry()` validates `entry_date` format, that every line references a real account (via a live query against `accounts`), that each line sets exactly one of debit/credit as a positive amount, and that debit and credit totals match exactly in cents; saves the entry, its lines, and an audit-log entry atomically. Wired `POST /journal-entries` into `src/app.ts`.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live via `curl` (seeding real accounts first) for the happy path (confirmed 250.75 converts to exactly 25075 cents on both lines), an unbalanced entry, and a nonexistent-account reference, then confirmed via direct SQLite query that only the successful entry persisted (1 journal_entries row, 2 journal_lines rows, 1 audit entry); `tests/journalEntries.test.ts` (vitest + supertest, in-memory DB) — 4/4 passing, covering the happy path and all three failure paths named in the story (unbalanced entry, invalid account, database save error); full suite — 14/14 passing, no regression.
  - Notes: **Bug found and fixed during this story's own testing, not deferred.** `validateJournalEntryInput`'s account-existence check queries the DB directly, but the `/journal-entries` route only wrapped the *create* step in `try/catch`, not validation — so a DB failure during validation would bypass our error handling and hit Express's default handler (risking leaked internals) instead of the intended generic 500. Fixed by wrapping validate+create in one `try/catch`. Found the identical shape of bug already present in `/accounts` (`validateAccountInput`'s `parent_account_id` check has the same DB-touching-inside-validation pattern) and fixed that too, with a new regression test (`tests/accounts.test.ts`, now 6 tests) — this was a small, same-story-guarantee fix (the "no leaked internals on DB error" requirement both stories share), not unrelated scope creep, and the user explicitly approved fixing it before proceeding.

- [x] STORY-004: post transactions to the general ledger
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: Pre-corrected `.colaberry/plan.json`'s STORY-004 placeholder criteria to the real brief's exact wording before writing any code. Confirmed a design read with the user first: "posting" is a distinct lifecycle step from journal-entry creation (STORY-003), not the same action — standard double-entry accounting pattern, and needed so STORY-005 (trial balance) and STORY-006 (financial statements) have a clean posted ledger to report from rather than every draft ever created. Added `status` (`draft`/`posted`) and `posted_at` columns to `journal_entries` in `src/db.ts`, plus a new `general_ledger` table (one row per posted line); added a backward-compatible `addColumnIfMissing()` migration helper so a database created before this change still gets the new columns via `ALTER TABLE` rather than breaking — verified against a simulated pre-STORY-004 database through the real `openDb()` path, not just a fresh one. Added `src/generalLedger.ts`: `postJournalEntry()` re-validates balance and account existence at posting time independent of creation-time checks, and is idempotent by design — posting an already-posted entry returns the identical result again with zero new writes, rather than erroring or double-posting. Wired `POST /journal-entries/:id/post` into `src/app.ts`.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live via real HTTP calls (seed accounts → create draft entry → post → post again → post nonexistent id), then confirmed via direct SQLite query that the double-post left exactly 2 `general_ledger` rows and 1 `audit_log` entry, not 4 and 2; `tests/generalLedger.test.ts` (vitest + supertest, in-memory DB) — 5/5 passing, covering the happy path, the idempotent double-post as its own explicit test (not just assumed from the happy path), and all three failure paths named in the story (invalid/nonexistent transaction, unbalanced posting via a DB-level tampered draft that bypasses creation validation on purpose, database error); full suite — 19/19 passing, no regression.
  - Notes: No accounting-period/period-closing concept exists yet in this codebase, so "valid accounting periods" from the shared guardrail (REQ-009) isn't enforced by this story — only balance and account validity are re-checked at posting time. Flagging this now rather than inventing a period-locking feature nobody asked for; worth confirming whether a later story (STORY-018, which also fulfills REQ-009) is expected to add it.

- [x] Add three project skills, README, and a recording of each firing
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: Created `.claude/skills/api-smoke-test/SKILL.md` (runs every route live end-to-end, `Bash`+`Read`+`Grep`), `.claude/skills/plan-progress-audit/SKILL.md` (whole-repo drift sweep across `plan.json`/`progress.json`, `Read`+`Grep` only after narrowing away an initially over-granted `Bash`), and `.claude/skills/release-boundary-retro/SKILL.md` (end-of-release design reflection, `Read`+`Grep` only). Added `.claude/skills/README.md` summarizing all three with a worry-based disambiguation guide, revised after a self-test using only the README (pretending no prior repo knowledge) surfaced that a vague phrase could plausibly match two skills — fixed by naming the underlying worry (correctness vs. maintainability vs. liveness) each skill answers, not just its trigger phrases. Recorded and committed `artifacts/week-02/skills-demo.mp4` (~50MB, under GitHub's 100MB limit) showing all three skills firing from plain requests that never name them, embedded near the top of the README.
  - Verification: All three skills confirmed firing live in-session on unprompted requests (not scripted invocations) — `api-smoke-test` and `plan-progress-audit` both ran for real against the actual database and produced genuine results (smoke test: 6/6 routes passing; audit: no drift found across STORY-000–004, one minor cosmetic gap noted). File size confirmed at 52,083,917 bytes before commit.
  - Notes: `plan-progress-audit` initially shipped with `Bash` access it didn't need (only used for a JSON-parse sanity check that `Read` already covers) — caught during the exercise's own "do any two skills share tool access, and is that a mistake" review and narrowed before committing. `release-boundary-retro` has not been run yet — r0 is not actually finished (only STORY-001–005 of its 8 stories are done), and the user has not yet said whether to run it early, scoped, or wait; nothing to log until that happens.

- [x] STORY-005: generate a trial balance
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: Pre-corrected `.colaberry/plan.json`'s STORY-005 placeholder criteria (and a slightly mismatched narrative "so that" clause) to the real brief's exact wording before writing any code. Added `src/trialBalance.ts`: `generateTrialBalance()` aggregates `general_ledger` only (a `JOIN`, not the draft `journal_lines` table) grouped by account, returning per-account debit/credit totals and a grand total; with zero posted transactions it returns an empty account list and zero totals rather than erroring, directly satisfying the zero-balance acceptance criterion. Every call writes its own `audit_log` entry — confirmed with the user first that a `GET` with this per-call side effect doesn't violate the idempotency guardrail, since each report generation is a distinct legitimate event, not a retry of one business operation. Wired `GET /trial-balance` into `src/app.ts`.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live via real HTTP calls for both the zero-transactions case and the posted-transactions case (confirmed debit/credit totals matched exactly), then confirmed via direct SQLite query that 3 real `GET` calls produced exactly 3 `audit_log` entries; `tests/trialBalance.test.ts` (vitest + supertest, in-memory DB) — 4/4 passing, covering both acceptance-criteria scenarios, a database-error failure path, and an extra check confirming draft/unposted entries are excluded from the balance; full suite — 23/23 passing, no regression.
  - Notes: "Calculation error" and "display error" from the story's failure-paths list are both handled as the same generic 500 — there is no frontend yet, so "display" is read as the API response itself, not a separate UI layer. Flagged this reading to the user rather than assuming silently.

- [x] STORY-006: produce financial statements
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: Confirmed a design read with the user first: "incomplete data" means no company profile or an empty chart of accounts (real prerequisites), distinct from "accounts exist but nothing posted," which produces a valid all-zero statement like STORY-005 did. Pre-corrected `.colaberry/plan.json`'s STORY-006 placeholder criteria and role (was "financial manager," brief says "accountant") before writing code. Added `src/financialStatements.ts`: `generateFinancialStatements()` aggregates `general_ledger` by account type into an income statement (revenue − expenses) and a balance sheet, folding current-period net income into equity as its own line so Assets = Liabilities + Equity + Net Income holds — the standard basic/interim-statement presentation before formal closing entries exist. Wired `GET /financial-statements` into `src/app.ts`.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live via real HTTP calls — incomplete-data rejection, then a fully seeded scenario (company + 4 accounts + 3 posted transactions) confirmed the balance sheet actually balances ($1,300 assets = $1,000 capital + $300 net income); `tests/financialStatements.test.ts` (vitest + supertest, in-memory DB) — 5/5 passing, covering both incomplete-data variants, the valid zero-activity case, the happy path (checking the balance identity against real numbers), and a database-error path; full suite — 28/28 passing, no regression.
  - Notes: This is the last of r0's "core 6" stories (STORY-001–006); STORY-018 and STORY-019 are still outstanding before r0 as a whole is done.

- [x] Reconcile the portal's first real sync with STORY-006
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: The portal pushed its first real sync commit directly to `main` (`Colaberry Build Bot`, 29 files) while this session was mid-commit on STORY-006, causing a rejected push. Inspected the portal's commit before merging (per this repo's own git-safety habits): it correctly showed STORY-000–005 as `verified` with real commit SHAs and points awarded — confirming the webhook has been working all along and that the `plan.json` criteria text this session had been maintaining matched exactly what the portal expected. It did not touch `plan.json` (session-authored `plan.json` is now permanently "ours to maintain," per the portal's own new `CLAUDE.md` text: once a file diverges from what the platform wrote, it stops overwriting that file). It substantially rewrote `progress.json` to a richer per-story schema (`files_touched`, `tests_added`, `notes`, a portal-computed `verification` block) and now fully owns `manifest.json` (added `plan_sha256` and a per-file hash list for drift detection) on every sync going forward. Resolved the merge by taking the portal's versions of both files as the base and reapplying STORY-006's criteria/files_touched/notes on top, rather than trying to preserve this session's older flat schema. Updated `directives/story-completion-checklist.md` to reflect: `files_touched`/`tests_added`/`notes` are ours to fill (confirmed empty even on portal-verified stories), `verification` is portal-computed once synced, `manifest.json` is no longer hand-edited once the portal owns it, and to `git fetch` before every push since the portal can push its own commits at any time.
  - Verification: All four `.colaberry/*.json` files (`plan`, `progress`, `manifest`, new `profile`) parsed as valid JSON after the merge; `tsc --noEmit` and the full test suite (28/28) re-run after the merge commit to confirm nothing broke; `git fetch` showed no further remote commits before the final push, confirming the merge was pushed cleanly.
  - Notes: This validates the entire session's approach in `directives/story-completion-checklist.md` — the plan.json corrections made ahead of the portal's real provisioning were not wasted effort; the portal's own verification data matches them exactly.

- [x] STORY-018: ensure transactions follow balanced debit and credit rules
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: Flagged a real inconsistency in the brief itself first — its "Where this sits in the build" section listed STORY-016/017 (r4, multi-branch/import-export) as prerequisites, which don't exist yet and have nothing to do with balance validation; proceeded on the real dependency graph (STORY-002/003/004) instead of building around the mislisted ones. Found that balanced/unbalanced posting validation already existed from STORY-004 (`findPostingErrors`); the real gap was the Trust criterion — only successful postings were logged to the audit trail, so a rejected attempt (unbalanced, invalid account, nonexistent entry) left no trace at all. Added `logRejectedAttempt()` to `src/generalLedger.ts`, logging a `transaction_post_rejected` audit entry (with the specific reasons) before throwing either rejection error, while leaving the existing success-logging and idempotent-repost behavior (no duplicate entry on a harmless re-post) exactly as STORY-004 built it.
  - Verification: `tsc --noEmit` passes; a standalone script confirmed exactly 2 rejected-attempt audit rows (not-found, unbalanced) and exactly 1 posted-success row across a sequence of 4 calls including an idempotent re-post; `tests/generalLedger.test.ts` gained explicit assertions on both existing failure-path tests (checking the new audit rows) plus a new test directly proving both a success and a failure attempt are each logged; full suite — 29/29 passing, no regression (this also re-confirms STORY-004's exact-count audit assertions still hold with the new logging added).
  - Notes: **Scope decision made explicitly with the user, not assumed.** This story's failure-paths list names "closed accounting periods" and REQ-009 mentions period validation, but no period concept exists in this codebase and none of the 3 actual acceptance criteria test it — the user chose to keep scope to what the stop condition requires rather than build a periods feature nobody asked for. This gap remains open, same as flagged in STORY-004's notes. Also: `progress.json`'s own STORY-018 entry, now portal-provisioned, already carried the correct criteria text independently of `plan.json` — first confirmation that the portal generates progress.json's criteria from its own source (likely `docs/stories/STORY-018.md`) rather than from this repo's `plan.json`, so `plan.json` corrections from here on are for the Command Center's own consistency, not for portal verification.

- [x] Update story completion checklist with the portal-generated-criteria discovery
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: Added a note to `directives/story-completion-checklist.md`'s "Why this exists" section documenting that, once the portal has provisioned a repo, `progress.json`'s criteria come from the portal's own source rather than `plan.json` — but `plan.json` fixes are still worth doing for Command Center consistency and for the pre-sync/no-push-access case.
  - Verification: File committed and pushed (`4646973`); content directly reflects the STORY-018 observation confirmed moments earlier.

- [x] STORY-019: provide an audit trail for all financial transactions
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: Surfaced two real blockers before writing any code, resolved with the user rather than routed around: (1) no user/auth concept exists anywhere in this codebase, yet the Trust criterion literally requires "a timestamp and user ID" per entry — the user introduced the term "compatibility mode" for the fix: build the permanent shape now (a real `user_id` column on `audit_log`, a `getUserId()` helper reading an `X-User-Id` header defaulting to `"system"`) so a future auth story only has to populate it, not retrofit the schema; (2) "unauthorized access to audit trail" is a listed failure path with no corresponding acceptance criterion — treated as out of scope, same reasoning as STORY-018's accounting-periods question. User then explicitly chose NOT to limit `user_id` threading to just journal-entry/posting events — applied it to all six service modules (`companies`, `accounts`, `journalEntries`, `generalLedger` x2, `trialBalance`, `financialStatements`) for one consistent `audit_log` shape, since REQ-008's guardrail says "all financial transactions." Added `src/auditTrail.ts` (`GET /audit-log`, optional `entity_type`/`entity_id` filters): derives a `success`/`failure` status from the action name, and defensively parses stored `details` JSON so a malformed or missing row returns `null` there instead of crashing the whole request.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live via real HTTP calls — a successful posting and a failed attempt, each with a different `X-User-Id`, both read back correctly through `/audit-log?entity_id=...` with the right status, user id, and details; `tests/auditTrail.test.ts` (vitest + supertest, in-memory DB) — 4/4 passing, covering both view scenarios, the default-`"system"`-actor Trust criterion, and the defensive-parsing failure path (simulated malformed and null `details` rows); full suite — 33/33 passing, no regression.
  - Notes: Also corrected `plan.json`'s STORY-019 placeholder criteria and narrative role (was "accountant," real brief says "auditor") — added "auditor" to `plan.derived.roles` since it's a genuinely new role. This story completes r0's core 8 stories (STORY-001–006, 018, 019) — r0 is now actually done, making the previously-deferred release-boundary retro meaningful for the first time.

- [x] Release-boundary retro: split app.ts into routers, consolidate audit-log writes
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: Ran the `release-boundary-retro` skill now that r0 is genuinely complete (deferred from after STORY-003, per the cadence agreed then). Found two owner-collapse patterns: `app.ts` held every route across all three owning agents (Product Owner, Accounting System, Audit System) in one growing file importing from every service module; and `INSERT INTO audit_log` had grown from 3 duplicated call sites (flagged at the STORY-003 retro) to 6 by r0's end, since nothing acted on it in between. User explicitly confirmed no upstream r1 story fixes the duplication — every remaining story (007–010, 020) carries its own audit-trail criterion, so it would only have kept growing. Split `app.ts` (199 lines, 7 routes) into `src/routes/{companies,accounts,ledger,reporting,audit}.ts`, each an Express Router factory taking `db` and owning one domain's routes — `app.ts` now just mounts them (24 lines). Consolidated all 6 duplicated audit-log inserts into `src/auditLog.ts`'s `recordAuditEvent()`, the sole place `audit_log` is written from now.
  - Verification: `tsc --noEmit` passes; full test suite — 33/33 passing, unmodified, proving the split is a pure restructure (every test drives requests through `createApp()`, indifferent to which file a route lives in); live server smoke-check hit one route from each of the 5 new routers (health, companies, audit-log) with correct status codes; `grep -r "INSERT INTO audit_log" src/` confirmed exactly one match, down from 6.
  - Notes: This is not a story commit — no `STORY-XXX:` trailer, since it's infrastructure hardening from the retro process, not part of the story queue. Second finding from the same retro (accepted but not required to fix now): `generalLedger.ts` absorbed three different owning stories' concerns over time (STORY-004 posting, STORY-018 balance hardening, STORY-019 audit threading) — left as-is since it wasn't part of the boundary chosen to act on this round.

- [x] STORY-007: manage customer and vendor profiles
  - Date: 2026-09-08
  - Session: CC-20260907-ri4y
  - What changed: First story of r1. Pre-corrected `.colaberry/plan.json`'s STORY-007 placeholder criteria and narrative role (was "sales manager," brief says "business manager") before writing any code. Confirmed a design read with the user: "duplicate profile" (a named failure path, not literally tested by the 3 acceptance criteria, but cheap enough to build unlike STORY-018's accounting-periods situation) uses a `(type, name)` natural key — the same name is allowed across a customer and a vendor profile since they're different relationships, not twice within the same type. Also asked the user to choose between two type-specific routes (`/customers`, `/vendors`) versus one shared route with `type` in the body — went with two routes to match this codebase's existing per-concept convention (`/companies`, `/accounts`, `/journal-entries`); each route pins its own `type` server-side rather than trusting the request body, closing off a "wrong type sent to the wrong endpoint" mistake class. Added `src/businessPartners.ts` and `src/routes/businessPartners.ts`; writes go through `recordAuditEvent()`, the consolidated helper from the release-boundary retro — the first new story built after that refactor, confirming it holds up cleanly for new domains.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live via `curl` for 5 scenarios (customer created, vendor with same name succeeds, duplicate customer rejected with 409, missing-field 400, and the type-pinning safeguard — a `type: "vendor"` sent to `/customers` still creates a `customer`); `tests/businessPartners.test.ts` (vitest + supertest, in-memory DB) — 6/6 passing, covering the happy path, invalid details, duplicate `(type, name)`, same-name-different-type success, the type-pinning safeguard, and a database-error path; full suite — 39/39 passing, no regression.
  - Notes: Adding this router was a one-line change to `app.ts` (`app.use(businessPartnersRouter(db))`) rather than a re-edit of a growing file — direct payoff from the retro refactor two commits earlier.
