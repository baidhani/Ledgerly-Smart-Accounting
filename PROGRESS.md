# Progress

- [x] STORY-000: build the Command Center
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Registered the GitHub push webhook to enterprise.colaberry.ai; created `.colaberry/plan.json`, `.colaberry/progress.json`, `.colaberry/manifest.json` (none existed yet, so they were authored from the plan/requirements/stories/releases given for this project) and `docs/stories/STORY-000.md`; built a 9-tab static Command Center (`index.html`, `assets/app.js`, `assets/style.css`) that reads those three JSON files at runtime — Overview, Outcomes (empty state, no measures defined), Users & use case, Guardrails (REQ-007/008/009/021 enforcement status), Systems (empty state, none named), Project management (Gantt + task table), AI agents (built from story owners, since no scoped agent roster exists yet), Knowledge base (requirement/story traceability + a data-only Q&A panel), and Data model (proposed entity list, explicitly a starting point). Sample/Real toggle only fabricates data where real data doesn't exist yet (Overview totals, Outcomes measures) — all other tabs show real plan/progress content unlabelled, to avoid mislabeling real data as sample.
  - Verification: All three `.colaberry/*.json` files parse as valid JSON (`python -m json.load`); `assets/app.js` passes `node --check`; page manually verified serving correctly at `http://127.0.0.1:5500/` after diagnosing that port 8000 was reserved by an unrelated Windows system service (PID 4 / Microsoft-HTTPAPI); user confirmed the page loaded and asked to "build the rest".
  - Notes: Criteria 3 and 4 in `.colaberry/progress.json` (plan/progress/manifest "committed in this repo") were ticked `true` immediately before this same commit, so the claim is accurate at push time. Deviation: `.colaberry/plan.json` and `progress.json` are described in the assignment brief as portal-owned/rewritten-on-sync files, but neither existed in the repo, so they were authored locally to unblock the build; a future portal sync overwriting them is expected, not a bug. The AI agents and Data model tabs use small hardcoded structural lists (story-owner roster, proposed entity/field list) per the assignment brief's explicit instruction that this data isn't in plan.json yet — this does not hardcode plan/progress *values* (requirements, stories, releases, verification state), which are all read from the JSON files at runtime.

- [x] STORY-000: fix GitHub Pages 404 on .colaberry/*.json
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Added `.nojekyll` at the repo root. GitHub Pages defaults to Jekyll processing, which silently excludes any dotfile/dot-folder (including `.colaberry/`) from the published build — the live site was 404ing on `.colaberry/progress.json` even though it was committed and served fine locally.
  - Verification: User confirmed the Command Center loaded on GitHub Pages and reported the exact 404 error text from the Overview tab's own error state, which named `.colaberry/progress.json (404)` — root-caused to Jekyll's default dotfile exclusion.
  - Notes: Pages will need to rebuild after this push before the fix takes effect (typically under a minute).

- [x] STORY-001: create and configure a company profile
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Scaffolded a Node/TS/Express backend (`package.json`, `tsconfig.json`, `src/index.ts`) with a `/health` route; added a SQLite database layer (`src/db.ts`, WAL mode, `companies` + `audit_log` tables, migrated on startup) wired into the app via dependency injection (`app.locals.db`); added `POST /companies` (`src/companies.ts`) which validates required fields and formats (ISO date, 3-letter currency code), saves the company and an audit-log entry atomically in one DB transaction, and returns a generic 500 with no leaked internals on a database save failure. Closed a moderate/high/critical `qs`/`body-parser`/`express` CVE chain via a `qs` override in `package.json`.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live with `curl` for the happy path, missing fields, invalid formats, and confirmed via direct SQLite queries that rejected requests wrote zero rows while the accepted one persisted both a `companies` row and its `audit_log` entry; `tests/companies.test.ts` (vitest + supertest, in-memory DB) — 4/4 passing, covering the happy path and all three failure paths named in the story (incomplete data, invalid format, database save error).
  - Notes: Left 5 `vitest`-bundled `esbuild`/`vite` advisories unaddressed — dev-only test-runner surface, not shipped to production; fixing them means an unprompted breaking `vitest` major-version bump. No idempotency key on `POST /companies` — the story's acceptance criteria don't call for one and none of this org's idempotency example patterns (email/webhook/lead-capture style dedup) apply to a first-time profile creation; flagging in case a future story expects duplicate-submission protection.

- [x] STORY-001: sync .colaberry plan/progress with the story's real acceptance criteria
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: `.colaberry/plan.json`'s STORY-001 entry still carried a placeholder single criterion and the wrong narrative role ("business manager") left over from the earlier Command Center scaffolding, not the actual story brief. Replaced with the three exact acceptance-criteria lines and the accountant narrative from this story's real brief, and ticked all three `true` in `.colaberry/progress.json` (`verification.state: "submitted"`, `commit: "5090306"`) so the portal — which matches criteria by exact text — can recognize STORY-001 as done rather than showing it stuck at a criterion it never actually had.
  - Verification: All three `.colaberry/*.json` files parse as valid JSON after the edit; user asked directly whether the portal would show STORY-001 as complete, which is what surfaced the text mismatch.
  - Notes: This is a reminder that `.colaberry/plan.json`'s stories were originally authored locally (see the STORY-000 entry above) before the portal's own story briefs existed on disk — each subsequent story should be checked against its real brief for narrative/criteria drift before assuming the placeholder is accurate.

- [x] Add story completion checklist directive
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Added `directives/story-completion-checklist.md`, a repo-level SOP any agent (not just this session) reads before completing a story and pushing to trigger portal verification. Documents the STORY-001 lesson as a repeatable procedure: check `plan.json`'s acceptance criteria against the real story brief word-for-word before touching `progress.json`, verify each criterion live before ticking it, validate the JSON files parse, and follow the commit/PROGRESS.md conventions this repo already requires.
  - Verification: File committed and pushed (`755372b`); content reviewed against the actual STORY-001 incident it documents.
  - Notes: User specifically wanted this discoverable by any agent working the repo, not just carried in this assistant's private cross-session memory — hence a repo file under `/directives` (per this repo's own `CLAUDE.md` folder responsibilities) rather than only a memory entry. A corresponding feedback memory was also saved for this assistant's own future sessions, cross-referencing this file.

- [x] STORY-002: create and maintain a chart of accounts
  - Date: 2026-09-07
  - Session: CC-20260907-ri4y
  - What changed: Followed `directives/story-completion-checklist.md` for the first time end-to-end: checked `.colaberry/plan.json`'s STORY-002 entry against the real story brief *before* writing any code, found the same placeholder-criteria mismatch STORY-001 had, and fixed it up front instead of after the fact. Added an `accounts` table to `src/db.ts` (`UNIQUE` constraint on `code`, self-referencing `parent_account_id` FK). Added `src/accounts.ts`: `createAccount()` validates required fields, account `type` against the five standard categories (asset/liability/equity/revenue/expense — a domain-standard assumption, not in the brief, logged here), and `parent_account_id` referential validity; saves the account and an audit-log entry atomically; a duplicate `code` throws a typed `DuplicateAccountError` caught by the route as 409, instead of leaking a raw SQLite constraint error. Wired `POST /accounts` into `src/app.ts`.
  - Verification: `tsc --noEmit` passes; server manually booted and exercised live via `curl` for the happy path, invalid type, missing fields, and duplicate code, then confirmed via direct SQLite query that only the one successful request persisted (1 account row, 1 audit entry); `tests/accounts.test.ts` (vitest + supertest, in-memory DB) — 5/5 passing, covering the happy path and all three failure paths named in the story (invalid details, duplicate account, database save error); full suite (`tests/companies.test.ts` + `tests/accounts.test.ts`) — 9/9 passing, no regression.
  - Notes: Because `plan.json` was corrected before implementation this time (commit `5acc388`), there was no STORY-001-style text mismatch to fix afterward — `progress.json` was ticked and pushed in a follow-up commit (`1d78f8a`) referencing `5acc388` as the verifying commit.
